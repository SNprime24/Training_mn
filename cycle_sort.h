#ifndef CYCLE_SORT_H
#define CYCLE_SORT_H

#include <vector>

// Sorts arr in ascending order in-place using cycle sort. O(n^2) time, O(1) extra space.
void cycle_sort(std::vector<int> &arr);

#endif
